<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>PresalesHead_Field_Restrictions</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
     $Profile.Id = &apos;00egK000001qs5N&apos;,
    OR(
        ISCHANGED(Amount),
        ISCHANGED(AccountId),
        ISCHANGED(OwnerId),
        ISCHANGED( Name ),
        ISCHANGED(CloseDate)
        
    )
)</errorConditionFormula>
    <errorMessage>&quot;You are only allowed to Edit Presales_Related fields.&quot;</errorMessage>
</ValidationRule>
